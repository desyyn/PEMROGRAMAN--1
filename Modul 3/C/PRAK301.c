#include <stdio.h>
int main() {
    int angka_pertama, angka_kedua;
    printf("Input\n");
    scanf("%d" "%d", &angka_pertama, &angka_kedua);
    printf("Output\n");
    if (angka_pertama < angka_kedua) {
        printf("%d %d", angka_pertama, angka_kedua);
    } else {
        printf("%d %d", angka_kedua, angka_pertama);
    }
return 0;
}
